# 0.2 overlay

Agent law for Linear project **RV 0.2 — Auto-review Alpha**. Human picture: `docs/architecture/rv-02.html` (not law).

**Arbiter:** this file wins for ProposedAction, Ask, Auto-review, HardPolicyDecision, PendingApproval, Mac app, harness. `docs/factory/PLAN.md` + `STATUS.md` win for the v1 hook guard (packs, Decision, C pipe, setup/doctor). `MODULES.md` still owns the hexagon and **what already exists** — read it before treating § Order as a blank ticket.

Living now/next: `docs/factory/STATUS.md`. Do not start factory T-tickets or `planning/` product-takes from this overlay.

Done for any 0.2 change: named module compiles; `evaluate()` still returns `Decision`; Ask is not a `Decision` case; packs still deny `git reset --hard`.

## Strangler

Keep the hook door. Insert IR and hard policy in front of packs.

```
HostCodec → ProposedAction.shell → analyze → HardPolicyEngine
                ↘ evaluate() on .unknown or as AND-deny
         → HardPolicyDecision
              ask + Ask-capable host → ApprovalBridge.hostNative
              reviewEligible → ActionReviewer (after host Ask exists)
              ask + other host → v1 unlock (TTY grant / deny), not silent allow
```

Packs are **fallback**, not the product model. **Host Ask comes before Auto-review.** AI runs only on `.reviewEligible`, and only after that host already has a working Ask path. Foundation Models stay out of RVDomain / RVEngine / RVPolicy.

### Host Ask

Ask-capable (0.2): **Pi, OpenCode, Claude, Hermes**. `ApprovalContinuation.hostNative` (or resume/defer the host already supports). Hard policy emits `.ask`; the host pauses; Allow once / Deny resolve through RV.

Not Ask-capable in this slice: **Grok** (v1 codec stays deny-or-TTY). **Codex** and **Cursor** are not in the Ask set until named.

Pi / OpenCode / Claude / Hermes have spend-then-allow on the hook door (MODULES.md). Grok stays deny-or-TTY. Do not bolt AFM onto a missing pause path. Do not treat `host-ask-plan.md` as current (“no Ask code”).

v1 `PLAN.md` still has no host Ask. That lock is the hook-guard. This overlay replaces it for 0.2.

## Types (RVDomain)

Closed world = enum. Open world = protocol at RVService only.

```swift
enum ProposedAction { case shell(ShellAction) }   // fs/net/mcp reserved, unused
struct ShellAction { command, cwd, host, session, fingerprint }
enum SemanticAnalysis { case git, filesystem, wrapper(inner:), unknown }
enum HardPolicyDecision { case allow, ask, deny(Deny), reviewEligible }
enum PolicyPredicate { /* typed AST, not regex */ }
enum ApprovalDecision { case allowOnce, createRule(PolicyDraft), deny }
```

Newtypes: `ActionFingerprint`, `SessionID`, `ApprovalID`, `RepositoryRoot`.
Grant key is fingerprint + session + cwd. `matchingView` remains the pack-fallback key.

Protocols (service edge, `any` ok): `ActionReviewer`, `ApprovalBridge`.
Keep `HostCodec`. Analyzers are concrete functions — Git/FS are a closed set.

`HardPolicyDecision.reviewEligible` is a kind of Ask, not Allow. A reviewer `allow` cannot type-check into `.deny`.

## Modules

Place the type, then the function. New closed-source targets do not import into the OSS core.

| Work | Module |
|---|---|
| IR, fingerprints, HardPolicyDecision, PolicyPredicate, Approval*, AuditEvent shape | RVDomain |
| `analyzeGit` / `analyzeFilesystem` / wrapper recursion; existing `evaluate()` | RVEngine |
| TypedRule, EffectivePolicy, invariants, PendingApprovalStore actor, fingerprint grants | RVPolicy |
| Catalog JSON | RVPacks (unchanged) |
| HostCodec stays thin; ApprovalBridge; codecs emit ProposedAction | RVHooks |
| Assemble analyze → hardPolicy → reviewer → pending → bridge; new IPC | RVService |
| Append-safe AuditEvent | RVHistory (stop stubbing when 166 lands) |
| explain / history / inspect / policy show | RVCLI |
| Menu bar, Island, `rv <agent>`, AFM adapter | new closed-source targets; IPC clients only |

EffectivePolicy merge: invariants ⊳ machine ⊳ repo, restrict-only.

## Order

This section is the **execute queue**. Linear workflow (Backlog / In Progress) is not. Related/blocks edges on Linear are hints; if they disagree with this list, this list wins.

Serial until IR exists. Then the listed groups. Next ticket = first **unfinished** item below. Landed items stay on the list so agents do not restart them.

Gate is `tools/gate.sh <Module>Tests` plus the **Manual** line. Skip Manual only on 1–5 when the corpus already covers that fixture; never skip 6–9.

1. **Landed — OPE-156** IR + fingerprint through every HostCodec (`ProposedAction`, `ActionFingerprint.make`, `HostCodec.proposedAction`). Residual: default path is empty-effect shell; semantic Git/FS fingerprints stay distinct (`ProposedAction.swift`). Do not greenfield.
   Manual: one Pi (or Grok) hook of `git status` still allows; `git reset --hard` still denies. Inspect/debug-print ProposedAction has command, cwd, host, fingerprint.
2. **Landed — 254 → 255 → 256** analyzers + unwrap. **259** fixtures ship with each analyzer. See MODULES.md.
   Manual: `rv explain 'git checkout -b feature'` vs `rv explain 'git checkout -- file.swift'` differ. `bash -c 'git reset --hard'` matches direct. Quoted `rm -rf` text does not delete. Deep wrappers ask/deny, never allow.
3. **Landed — 157 + 159 / 160** hard policy, repo boundary, protected paths. `ActionPolicyEngine` emits `HardPolicyDecision`. 169 residual only if MODULES does not already cover Ask emit.
   Manual: force-push to `main` denies; normal feature-branch push does not. `rm ../outside` is outside-repo. `~/.ssh` stays protected when a repo rule tries to allow it. Mock reviewer `allow` cannot pass a hard deny.
4. **Landed (pack pipeline) — 168** `ExplainStep` for normalize → quick-reject → safe → destructive → default. Residual: semantic Git/scope rows on explain if not already shown.
   Manual: `rv explain 'git push --force origin main'` shows Git action, scope, matched rule, decision. Nested `bash -c` shows wrapper + inner. Unknown syntax says fallback, not a fake semantic hit.
5. **Open — 158** fingerprint grants (allow-once is still `{matchingView, cwd}`). **Partial — 170 / 171** `EffectiveActionPolicy` exists. **Open — 166 / 167** history (`RVHistory` is a stub).
   Manual: mint Allow once, identical retry consumes once, mutated command/cwd/session does not. `rv policy show` names built-in vs machine vs repo. `rv inspect <id>` matches what just happened.
6. **Partial — Host Ask:** **OPE-264 / OPE-265** Pi / OpenCode spend-then-allow plus Claude/Hermes Ask wire are in MODULES (confirm / DialogConfirm / `permissionDecision: ask` + PermissionRequest spend / Hermes `action: approve` after spend). Grok stays deny-or-TTY. Codex and Cursor are not in this Ask set. `docs/architecture/host-ask-plan.md` is a stale investigation — do not treat it as “no Ask code.”
   Manual (required, real hosts): on Pi, OpenCode, Claude, Hermes — Ask pauses in the host; Allow once runs exactly once; Deny never runs; replay does not. Grok same fixture denies or TTY, never allows. Wire proof for Claude/Hermes Ask+spend is on this machine; a human still needs to click Allow in the live TUI.
7. **Open — 246** PendingApproval for always-on / no-human — after host Ask works on the four. Types exist; do not greenfield the record shape.
   Manual: trigger Ask, quit app + rvd, relaunch, pending still there. Resolve once. Second resolve is a no-op. Timeout matches wait / auto-deny / fail-task.
8. **Open — 247 / 248** app. **251 / 240** harness.
   Manual: Island/notification with dashboard closed. No one-click Allow from notification. Always allow… shows typed-rule preview; cancel writes nothing. `rv claude` (etc.) keeps supervision after the terminal is gone. UI cannot authorize if rvd is down.
9. **Partial — 250** shadow review exists (does not change the live decision). **Open — 249 / 253 / 252** live Auto-review last, and only for Ask-capable hosts. Entitlement disables reviewer only. **258** after TypedRule exists.
   Manual: shadow log does not change the decision. After promote: review-eligible auto-allows; low confidence asks; hard deny still denies; Auto-review off restores Ask. Billing/entitlement down disables reviewer only — packs and hard policy still work. “Never allow force pushes to main” previews a typed Git deny before save.

Ticket done: types in the table above; tests in that module’s target; `tools/gate.sh <Module>Tests`; Manual line for that step actually run.

## Already in the repo (do not greenfield)

- `HookHost` includes `.claude`, `.openclaw`, `.hermes`, `.codex`, and `.cursor`. Grok / Pi / OpenCode / Claude / OpenClaw / Hermes / Codex / Cursor codecs exist. OpenClaw, Codex, and Cursor are host-only (no Ask). Claude and Hermes Ask is spend-first. Codex capability is deny-or-TTY; honor path is official older `decision: block` + stderr reason + exit 2. Cursor capability is deny-or-TTY; honor path is official native `permission: deny` + `user_message`/`agent_message` + exit 0. 163–165 are IR + bridge, not new adapters.
- `ProposedAction`, `ActionFingerprint.make`, and `HostCodec.proposedAction` exist. `unwrapCommand` peels `bash/sh/zsh -c` and interpreter one-liners (OPE-256). `Normalize` still strips sudo / env / command / backslash (max 32).
- `analyzeGit` / `analyzeFilesystem` / `ActionPolicyEngine` / `HardPolicyDecision` exist (254, 255, 157).
- `SecretPathCatalog` denies secret-shaped operands on the allow path. 160 is `FilesystemScope.protectedPath` + `builtin.action:protected-path-mutation` (MODULES.md).
- Allow-once is `{matchingView, cwd}`, TTY mint. 158 would widen that store — do not add a second ledger.
- `RVHistory` is a stub. 166 fills it. Shadow review (250) is not the audit log.
- Day-one packs: `core.git`, `core.filesystem`, `system.disk`.

## Implement one ticket

1. Next ticket = first unfinished item in § Order (skip **Landed**). Do not pick Linear In Progress. Do not pick a ticket unless the human names one.
2. Read this file, `MODULES.md`, that Linear ticket body.
3. Name the module from the table. Put the type in RVDomain before behavior.
4. Keep `evaluate() -> EvaluationResult` / `Decision` working. Product Ask is `HardPolicyDecision`.
5. Unknown syntax → `.unknown` → packs. Recursion/parse limits → ask or deny, not allow.
6. Closed-source UI talks IPC. It does not call `evaluate` or consume grants.
7. Gate the module you touched. Warm `.build`.
8. Run that step’s Manual line on a real host/TTY before calling the ticket done. Corpus is not a substitute for steps 6–9.

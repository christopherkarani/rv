You are a senior Swift 6.2 engineer implementing rv ticket T9 in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every acceptance checkbox in docs/factory/specs/phase-2-packs.md is proven.

Read in order: AGENTS.md, docs/factory/STATUS.md, `.grok/skills/swift-evaluate-parity`, docs/factory/specs/phase-2-packs.md, docs/factory/references/dcg-0.11.0-notes.md. Open phase-1-engine.md only if evaluate/PackID contracts are in doubt. PLAN.md wins product-law conflicts as arbiter.

STOP if T1 corpus is not green. After T1, T9 may run in parallel with T8. You MUST create a new git worktree from the T1-green SHA. Branch: feat/t9-catalog. Do not share a working tree with T8. You own catalog JSON, registry enable/disable, and rv packs. Do not add ArgumentParser or @main (T2 owns those). Do not change Package.swift products or dependencies. Do not enable extra packs by default. Do not write 99 Swift files. Do not add an env that a hook child honors to skip or select packs.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/. Tests use a temp HOME for config. Extract from a local --source-root already at tag v0.11.0. If the extractor count is not 99, stop and re-diff against the pinned 0.11.0 tree. Do not clone the upstream tree into rv.

Default-on remains core.git + core.filesystem only. system.disk stays off. Windows/Linux patterns are data, not a platform claim. PackID must accept IDs without a dot (strict_git, package_managers). A disabled pack must not deny; the same command denies after enable. Upstream CLI agree-rate is not a gate.

Prove each item:

- [ ] Dedicated worktree feat/t9-catalog
- [ ] 99 bundled pack documents, one decoder
- [ ] T1 corpus still green
- [ ] disabled extra pack cannot deny; enabled extra pack can
- [ ] core.* remain default-on; system.disk default-off
- [ ] rv packs lists and can enable/disable under a temp HOME
- [ ] no 99 Swift pack files; no file outside docs/factory/ contains the tokens dcg or ryk

End with a short human paragraph: what the catalog now contains, how much closer we are to v1 vs later parity, and what is still open. Do not commit unless the human asked.

**Maint ticket.** Current board: `docs/factory/STATUS.md`. Spec: `docs/factory/specs/phase-5-size-speed.md` (T14 section).

You are a senior Swift 6.3 engineer implementing rv ticket **T14** (one-shot evaluate) in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every T14 checkbox in that spec is proven.

Read in order: `AGENTS.md`, `docs/factory/STATUS.md`, `.grok/skills/swift-hook-xpc`, `docs/factory/specs/phase-5-size-speed.md`. PLAN.md wins product-law conflicts as arbiter.

**Blocked by: T11.** Do not start until `feat/t11-compile` is merged to the integration branch (or the human names a SHA that already contains T11). Branch: `feat/t14-oneshot` from that SHA. Do not share a working tree.

**Owns:** `Sources/RVIPC/IPCMethods.swift` (`EvaluateParams.clientSemver` only), `Sources/RVCLI/Service/XPCClient.swift`, `Sources/RVCLI/Service/ServiceClient.swift` (`evaluate` / `route` only), `Sources/RVService/ServiceRuntime.swift` (`acknowledge`, `handleIncoming`, `dispatch` / evaluate call site only), matching tests in `Tests/RVIPCTests`, `Tests/RVServiceTests`, `Tests/RVCLITests`.

**Must not:** edit T11 compile functions (`EvaluateSession` init compile set, `setPackEnabled` rebuild, `CoreWarmup`). Do not edit `GatedEvaluate` allowlist skip. Do not edit `Sources/rv/main.swift` or `install.sh`. Do not bump `rv.ipc.v1`. Do not add `--socket` to production `rvd`. Do not evaluate after a failed implicit hello.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/.

Additive: `EvaluateParams.clientSemver: String?` (`decodeIfPresent`). Hook `ServiceClient.evaluate` sends one XPC body (no prior `hello`) with `clientSemver = ProtocolVersion.serviceSemver`. If `handshakeOK == false` and that field is set, `handleIncoming` acknowledges using the request protocol + semver, then evaluates only when `ack.ok`. Skew / missing core → error, not evaluate. Old clients that Hello first and omit `clientSemver` stay valid. `status` / explain still Hello first. One-shot budget is 200+500 = 700 ms. Host adapter timeout stays 5 s.

Prove with `tools/gate.sh --quiet RVIPCTests RVServiceTests RVCLITests`. End with a short human paragraph: how many XPC performs a hook evaluate now takes, and that down/skew still deny `git reset --hard`. Do not commit unless the human asked.

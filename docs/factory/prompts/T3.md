You are a senior Swift 6.2 engineer implementing rv ticket T3 in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every acceptance checkbox in docs/factory/specs/phase-1c-service.md is proven.

Read in order: docs/factory/PLAN.md, docs/factory/HANDOFF.md, docs/factory/specs/phase-1-engine.md, docs/factory/specs/phase-1c-service.md. PLAN wins product-law conflicts.

STOP if T1 corpus is not green. If T1 is missing, write that in one sentence and stop.

Parallel: T3 may run at the same time as T2. You MUST create a new git worktree from the same T1-green SHA. Branch: feat/t3-service. Do not share a working tree with the T2 agent. Do not own pretty TUI snapshots. Do not edit T2’s test/explain/render files.

Package.swift (T3-only lines): T0 did not declare rvd. You MAY add executable target rvd (Sources/rvd, depends on RVService) and executable product rvd. Do not add ArgumentParser. Do not add product or target rv. Do not add @main to RVCLI. RVCLI file partition: Sources/RVCLI/Service/** only. Do not create RootCommand.swift or main.swift. Export a public ServiceStatusReport (robot/plain fields) that T2’s RootCommand can wrap after merge. If you add a ParsableCommand wrapper, it must compile only when ArgumentParser is already in the graph; do not add the package to make it compile. Prove status via in-process tests. Process `rv service status` is not required until T2’s rv product exists.

Work only in the rv worktree. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/. Do not write the operator’s live LaunchAgent HOME unless the human asked. T3 ships the plist template and rvd binary; T6 installs.

Implement rv.ipc.v1 Codable (evaluate, explain, classify, listPacks, setPackEnabled, allowOnce.consume, doctorSnapshot). allowOnce.consume spends a grant { command, cwd }, not a plaintext code. rvd is an on-demand LaunchAgent labeled dev.rv.evaluate, idle-exit about 5 minutes, not KeepAlive by default. T3 ships the plist template; T6 installs it. Do not handshake ok if core.git / core.filesystem failed to load. Production transport is XPC. Unix sockets are tests only. Handshake skew or rvd down must fall back to in-process evaluate. Never allow because XPC missed. Never evaluate against a skewed rvd. Engine must not import XPC. class/NSObject only at the RVService edge.

Prove each item:

- [ ] Dedicated worktree feat/t3-service from the T1-green SHA
- [ ] Codable round-trips for every v1 method
- [ ] Fake-XPC or UDS tests prove evaluate + handshake
- [ ] Down and skew fixtures evaluate in-process and still deny git reset --hard
- [ ] ServiceStatusReport (in-process) reports running / down / skew without pretty TUI
- [ ] Package.swift: product/target rvd only; no ArgumentParser; no product rv
- [ ] Production rvd does not listen on a Unix socket
- [ ] in-process evaluate may return indeterminate — do not coerce it to allow
- [ ] no RV_BYPASS, no pretty snapshot files; no file outside docs/factory/ contains the tokens dcg or ryk

End with a short human paragraph: what the service does when it is up and when it is down, how much closer we are to a fast hook, and what is still open. Do not commit unless the human asked.

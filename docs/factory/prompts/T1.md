You are a senior Swift 6.2 engineer implementing rv ticket T1 in /Users/chriskarani/CodingProjects/rv. Keep working until every acceptance checkbox in docs/factory/specs/phase-1-engine.md is proven. Do not stop at a plan.

Read in order: docs/factory/PLAN.md, docs/factory/HANDOFF.md, docs/factory/specs/phase-0-repo.md, docs/factory/specs/phase-1-engine.md, docs/factory/references/dcg-0.11.0-notes.md. PLAN wins product-law conflicts. The T1 spec wins API and corpus details unless it contradicts PLAN.

STOP if T0 is not done: swift test must already be green on the empty twelve-module graph and vendor/parity/PIN must read version=0.11.0. If T0 is missing, write that in one sentence and stop.

Work only in this repo. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/. Do not write live host dotfiles. Do not add SPM package dependencies. Do not add executables. The only allowed Package.swift edit is adding test target RVCorpusTests (depends on RVDomain, RVEngine, RVPacks). Do not start T2, T3, or hooks.

T1 is serial on feat/t1-engine or main after T0. Do not share a working tree with another agent.

Implement Domain newtypes, a closed Decision type, pure evaluate, ICU PatternEngine, and bundled JSON for core.git and core.filesystem only. Evaluation order is normalize → quick-reject → safe patterns first → destructive → default allow. Decision must make illegal states unrepresentable: a deny always carries ruleID + reason (PLAN’s associated-value shape). Do not ship a boolean isDenied. PackID accepts dotted and undotted IDs (`core.git`, `strict_git`, `package_managers`) via `^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)?$`. Do not use try! or force-unwrap on production paths, including PackID constants.

Day-one proof: git reset --hard → deny core.git:reset-hard. Confirm rule names from the pinned 0.11.0 pack source listed in the factory notes. Extract pack JSON from a local --source-root already at tag v0.11.0, or hand-extract; do not git clone into rv.

Severity mapping is pinned 0.11.0: critical/high deny; medium/low match but allow (stash-drop is allow + match). Quarantine stale SKILL.md rows that disagree with source. Leave semantic-only (?!) rules in JSON; do not write the Rust parsers. Never quarantine reset-hard.

Write corpus fixtures first (TDD), then make them pass. Each destructive pattern needs a deny fixture and a near-miss allow fixture. Role-aware quotes: git commit -m "git push --force" allows; "git" reset --hard and git reset '--hard' deny. Do not expand $TMPDIR as safe. Force-scan fork-bomb shapes. Empty/missing/unloadable core packs → Decision.indeterminate(.corePacksUnavailable). Do not invent a fake deny rule_id and do not default-allow. Command longer than 65_536 bytes → Decision.indeterminate(.commandTooLarge). PackID constants use a non-failable init(rawValue:), never PackID(rawValue:)!. Required deny rows include find -delete, unlink ~/.ssh/id_ed25519, redirect truncate of /etc/passwd, rm -rf ~, and git push -f. For git push -uf: deny only if the extracted 0.11.0 push-force-short regex matches; if it does not, record that in vendor/parity/EXTRACT.md — do not rewrite the regex.

Prove each item:

- [ ] T0 was already green
- [ ] swift test green including corpus / RVCorpusTests
- [ ] git reset --hard denies core.git:reset-hard
- [ ] git checkout -b feature, git restore --staged file, git clean -n, git push --force-with-lease allow
- [ ] git stash drop is allow + match (medium), not deny
- [ ] quoted destructive text does not deny
- [ ] /usr/bin/git reset --hard still denies
- [ ] only core.git and core.filesystem JSON exist and are enabled
- [ ] Package.swift gained RVCorpusTests only — no executables, no package dependencies
- [ ] oversize command → indeterminate(.commandTooLarge), not allow
- [ ] missing/empty core packs → indeterminate(.corePacksUnavailable), not allow, not a fake deny rule_id
- [ ] rm -rf ${TMPDIR}/build denies core.filesystem:rm-rf-general
- [ ] evaluate is pure: no Date, FileManager, ProcessInfo, TTY, XPC
- [ ] no RV_BYPASS; no file outside docs/factory/ contains the tokens dcg or ryk

End with a short human paragraph: what the engine now decides, the corpus pass/fail, how much closer we are to the day-one win (in-process deny is the critical path; hosts are not wired yet), and what is still open. Do not commit unless the human asked.

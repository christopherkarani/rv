**Maint ticket.** Current board: `docs/factory/STATUS.md`. Spec: `docs/factory/specs/phase-5-size-speed.md` (T12 section).

You are a senior Swift 6.3 engineer implementing rv ticket **T12** (hook argv fast-path) in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every T12 checkbox in that spec is proven.

Read in order: `AGENTS.md`, `docs/factory/STATUS.md`, `.grok/skills/swift-hook-xpc`, `docs/factory/specs/phase-5-size-speed.md`. PLAN.md wins product-law conflicts as arbiter.

**Blocked by:** none. Parallel with T10, T11, T13. Branch: `feat/t12-hook-dispatch`. Do not share a working tree.

**Owns:** `Sources/rv/main.swift`, `Sources/RVCLI/Hook/HookDispatch.swift` (create), `Tests/RVCLITests/HookDispatchTests.swift` (create).

**Must not:** edit `EvaluateSession`, `ServiceRuntime`, `RVIPC`, `install.sh`, `Package.swift` products. Do not add an executable. Do not add `RV_BYPASS`. Do not change codecs or `hostDenyText`. Do not delete `struct Hook: AsyncParsableCommand`.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/.

`Sources/rv/main.swift` order: `HelpDispatch.tryEmit` first, then `HookDispatch.matches` → `HookDispatch.run`, else `RV.main()`. `rv hook --help` stays HelpDispatch. Default host remains grok. `--host` / `--host=` parse matches today’s `Hook` command. Invalid host is a nonzero exit, no evaluate.

Prove with `tools/gate.sh --quiet RVCLITests RVHooksTests`. End with a short human paragraph: what `rv hook` no longer constructs, and that deny JSON / empty allow still match T4. Do not commit unless the human asked.

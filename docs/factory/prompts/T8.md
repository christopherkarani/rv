You are a senior Swift 6.2 engineer implementing rv ticket T8 in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every acceptance checkbox in docs/factory/specs/phase-3-allow.md is proven.

Read in order: AGENTS.md, docs/factory/STATUS.md, docs/factory/specs/phase-3-allow.md. Open phase-1-engine.md only if Decision/PolicyGate contracts are in doubt. PLAN.md wins product-law conflicts as arbiter.

STOP if T1 corpus is not green. After T1, T8 may run in parallel with T9. You MUST create a new git worktree from the T1-green SHA. Branch: feat/t8-allow-once. Do not share a working tree with T9. You own RVPolicy allow-once / allowlist / PolicyGate and CLI allow-once / allowlist. Policy types may land after T1; rebase CLI onto T2. Do not add ArgumentParser or @main (T2 owns those). Do not invent RV_BYPASS. Do not enable extra packs. Do not add Package.swift modules. Do not put a redeemable code on hostDenyText or hook JSON.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/. Tests inject a TTY probe; do not open a real TTY to prove a decision.

Unlock is: run the command in Terminal, or `rv allow-once <code>` on a TTY. No host Allow button. AllowOnceStore is an actor. Mint and redeem require a TTY. Consume is not interactive, matches { command, cwd }, and is single-use with atomic file CAS. IPC allowOnce.consume spends a grant, not a code. Config dir is $HOME/.config/rv only. Indeterminate is not allow. No plaintext code on disk. No raw command on disk. Clock is injected. No env the hook child honors to skip evaluate.

Prove each item:

- [ ] Dedicated worktree feat/t8-allow-once
- [ ] non-TTY mint/redeem refuse and do not write the store
- [ ] redeem then consume allows once; a second identical command denies
- [ ] no RV_BYPASS
- [ ] hostDenyText still has no code
- [ ] allowlist requires a reason
- [ ] PolicyGate: indeterminate → deny-for-incomplete-eval, not allow
- [ ] config dir is $HOME/.config/rv only — do not read XDG_CONFIG_HOME
- [ ] no file outside docs/factory/ contains the tokens dcg or ryk

End with a short human paragraph: how an operator unlocks a deny, how much closer we are to a usable deny-and-unlock loop, and what is still open. Do not commit unless the human asked.

**Historical ticket (shipped).** Do not use as session start unless replaying T5. Current board: `docs/factory/STATUS.md`.

You are a senior Swift 6.2 engineer implementing rv ticket T5 in the existing feat/t4-t5-hooks worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every T5 acceptance checkbox in docs/factory/specs/phase-1d-hosts.md is proven.

Read in order: AGENTS.md, docs/factory/STATUS.md, `.grok/skills/swift-hook-xpc`, docs/factory/references/host-contracts-v1.md, docs/factory/specs/phase-1d-hosts.md. PLAN.md wins product-law conflicts as arbiter.

STOP if T4 L3 Grok fixtures are not green. T5 is serial after T4 on the same worktree. Do not create a second worktree. Do not share the tree with T6/T7. Do not edit install.sh, setup/uninstall, or rv doctor.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/. Do not write the operator’s live ~/.pi or ~/.config/opencode. Tests use a temp HOME.

Implement Pi and OpenCode codecs plus rv-owned adapter templates. Pi: pi.on("tool_call") for evaluate, toolName bash, deny by returning { block: true, reason } where reason is hostDenyText. Display-only registerMessageRenderer for rv-decision must return { render(width) => string[] }, never a string. sendMessage on deny only, triggerTurn false. No confirm. No terminate: true. No user_bash. OpenCode: "tool.execute.before" only, tool bash, deny by throw new Error(hostDenyText). Display-only client.tui.showToast on deny (title RV · Blocked). Toast failure must still throw. No permission.ask. Adapters spawn `rv hook --host pi` or `--host opencode` with the RV adapter JSON in the spec. XPC down still evaluates in-process. Honor deny JSON regardless of exit code. A missing rv binary (ENOENT) blocks Pi/OpenCode with “rv missing.” Do not catch spawn failure and return nothing. A started rv that times out or crashes blocks with “rv failed.” Indeterminate from rv hook is deny JSON — adapters must honor it, not treat empty-reason as allow. Doctor reports a non-executable baked path as broken.

Write fixtures first, then codecs and templates.

Prove each item:

- [ ] T4 Grok fixtures still green
- [ ] Pi fixture: git reset --hard → block true + hostDenyText
- [ ] Pi fixture: non-bash tool is a no-op
- [ ] OpenCode fixture: git reset --hard throws hostDenyText
- [ ] OpenCode fixture: non-bash tool is a no-op
- [ ] missing rv (ENOENT) → Pi block / OpenCode throw “rv missing” (do not return nothing)
- [ ] deny JSON from rv hook is honored regardless of exit code, including indeterminate
- [ ] Pi display-only rv-decision renderer returns a component, never a string; OpenCode display-only toast on deny; no Allow UI
- [ ] no live dotfile writes; no file outside docs/factory/ contains the tokens dcg or ryk

End with a short human paragraph: which hosts can now block git reset --hard in fixtures, how much closer we are to the day-one win (all three host wires exist; setup is T6), and what is still open. Do not commit unless the human asked.

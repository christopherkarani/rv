**Historical ticket (shipped).** Do not use as session start unless replaying T4. Current board: `docs/factory/STATUS.md`.

You are a senior Swift 6.2 engineer implementing rv ticket T4 in /Users/chriskarani/CodingProjects/rv. Keep working until every acceptance checkbox for T4 in docs/factory/specs/phase-1d-hosts.md is proven.

Read in order: AGENTS.md, docs/factory/STATUS.md, `.grok/skills/swift-hook-xpc`, docs/factory/references/host-contracts-v1.md, docs/factory/specs/phase-1d-hosts.md. PLAN.md wins product-law conflicts as arbiter. Do not invent a Grok protocol.

STOP if T1 corpus is not green. T4 must not start before T1. Prefer a SHA that also has T2 hostDenyText. If T3 is merged, use the thin XPC client with in-process fallback. If T3 is not merged, evaluate in-process. Never allow because rvd is down or skewed.

Worktree: feat/t4-t5-hooks from the T1-green SHA (merge T2/T3 as needed). T5 is serial after T4 on this same worktree. Do not start T5 until T4 L3 fixtures are green. Do not share the tree with T6/T7. Do not edit install.sh, setup/uninstall, or rv doctor.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/. Do not write the operator’s live ~/.grok. Tests use a temp HOME.

Implement GrokHostCodec and `rv hook --host grok`. File event is PreToolUse / matcher Bash. Stdin is camelCase pre_tool_use. Shell tools are run_terminal_command, run_terminal_cmd, and Bash only.

Switch on Decision, never on `matched != nil` and never on `hostDenyText == nil` alone. Decision.allow (including medium/low match such as git stash drop) → empty stdout, exit 0. Decision.deny → {"decision":"deny","reason":"<hostDenyText>"} with exit 0. Decision.indeterminate (oversize/budget/core packs unloadable) → the same deny JSON with reason “rv could not finish evaluating this command. Run it in Terminal.” — not empty allow, even if hostDenyText is nil. Word is deny, not block. No Claude hookSpecificOutput. No pretty panels. No RV_BYPASS. No extra required stdout keys. On engine deny, rule_id lives inside reason via hostDenyText.

Write stdin/stdout fixtures first, then the codec.

Prove each item:

- [ ] T1 was already green
- [ ] allow git status → empty stdout, exit 0
- [ ] deny git reset --hard → deny JSON, reason equals hostDenyText, exit 0
- [ ] git stash drop → empty stdout, exit 0 (medium match is allow)
- [ ] oversize / indeterminate → deny JSON with the PLAN incomplete-eval sentence, exit 0 (not empty allow)
- [ ] non-shell Read/read_file is not evaluated
- [ ] malformed stdin is empty stdout, exit 0
- [ ] XPC down still denies git reset --hard in-process; if in-process returns indeterminate, still the deny JSON
- [ ] no live ~/.grok writes; no file outside docs/factory/ contains the tokens dcg or ryk

End with a short human paragraph: what Grok will now block, how much closer we are to the day-one win (Grok is the first host), and what is still open. Do not commit unless the human asked.

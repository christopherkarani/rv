**Maint ticket.** Current board: `docs/factory/STATUS.md`. Spec: `docs/factory/specs/phase-5-size-speed.md` (T11 section).

You are a senior Swift 6.3 engineer implementing rv ticket **T11** (enabled-only compile) in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every T11 checkbox in that spec is proven.

Read in order: `AGENTS.md`, `docs/factory/STATUS.md`, `.grok/skills/swift-evaluate-parity`, `docs/factory/specs/phase-5-size-speed.md`. PLAN.md wins product-law conflicts as arbiter.

**Blocked by:** none. Parallel with T10, T12, T13. Branch: `feat/t11-compile`. Do not share a working tree. T14 is blocked on you — do not edit `acknowledge` / `handleIncoming` / `dispatch` in `ServiceRuntime.swift`.

**Owns:** `Sources/RVService/EvaluateSession.swift`, `Sources/RVService/CoreWarmup.swift`, `Sources/RVService/ServiceRuntime.swift` (`init`, `setPackEnabled`, `gated` storage only), `Tests/RVServiceTests/EvaluateSessionTests.swift`, `Tests/RVServiceTests/EnabledCompileTests.swift` (new).

**Must not:** edit `GatedEvaluate` apply/peek bodies, `RVCLI`, `RVIPC`, `Sources/rv`, `install.sh`, `Package.swift`. Do not simplify regexes. Do not treat empty `enabledPacks` as a day-one refill. Do not quarantine `reset-hard` or `fork-bomb`.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/.

`EvaluateSession` still loads catalog snapshots as today, but `CompiledPacks.compile` only receives packs in the enabled set (`enabledPacks` argument, or `PacksFacade.effectiveIDs`, or `dayOnePackIDs`). Expose `package var compiledPackIDs: [PackID]`. After `setPackEnabled`, rebuild `GatedEvaluate(EvaluateSession(…))` so a newly enabled pack is compiled (otherwise it is a false allow). `gated` may become `var`.

Prove with `tools/gate.sh --quiet RVServiceTests RVEngineTests RVCorpusTests`. End with a short human paragraph: how many packs compile on a default session, and that `git reset --hard` still denies. Do not commit unless the human asked.

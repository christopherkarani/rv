**Maint ticket.** Current board: `docs/factory/STATUS.md`. Spec: `docs/factory/specs/phase-5-size-speed.md` (T10 section).

You are a senior Swift 6.3 engineer implementing rv ticket **T10** (release artifacts) in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every T10 checkbox in that spec is proven.

Read in order: `AGENTS.md`, `docs/factory/STATUS.md`, `.grok/skills/swift-hook-xpc`, `docs/factory/specs/phase-5-size-speed.md`. PLAN.md wins product-law conflicts as arbiter.

**Blocked by:** none. Parallel with T11, T12, T13. Branch: `feat/t10-release`. Do not share a working tree.

**Owns:** `tools/release.sh` (create), `install.sh`, `tools/README.md` (inventory row only), `docs/dev/SWIFT.md` (Release artifacts table only).

**Must not:** edit `Sources/`, `Tests/`, `Package.swift`. Do not add ArgumentParser. Do not set KeepAlive. Do not write the operator’s live `~/.local/bin` or live HOME. Do not `swift package clean` or wipe `.build`.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/.

Ship a **release** `rv` + `rvd` plus the SPM pack resource bundle(s) so a copied prefix still evaluates after the original `.build/…/*RVPacks.bundle` is hidden. `install.sh` copies that bundle when `RV_INSTALL_BIN` contains it. Staged binaries must not link `libswiftSwiftOnoneSupport`. Use `tools/swift-6.3.3 build -c release`. `strip -x` the products.

Prove each item from the spec T10 Prove list. End with a short human paragraph: what an operator copies now, measured staged sizes, and whether relocate-evaluate held. Do not commit unless the human asked.

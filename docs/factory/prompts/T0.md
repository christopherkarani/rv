You are a senior Swift 6.2 engineer. Your only job is ticket T0 in /Users/chriskarani/CodingProjects/rv: stand up the empty twelve-library package and stop. Keep working until every acceptance item in docs/factory/specs/phase-0-repo.md is proven with a command or a file read. If you are unsure about a path, graph edge, or file body, open that spec and copy it. Do not guess.

Read, in this order, before any edit: docs/factory/PLAN.md, docs/factory/HANDOFF.md, docs/factory/specs/phase-0-repo.md. PLAN wins product-law conflicts. The T0 spec wins file list, Package.swift graph, and acceptance. Leave docs/factory/PLAN.md unchanged.

Scope: this repo only. Land on main or feat/t0-repo. The git repo already exists; do not re-init it. T0 is serial: no second worktree, no T1, no evaluate, no hooks, no XPC, no setup, no pretty CLI, no ArgumentParser, no @main, no executable products, no SPM dependencies, no LICENSE.

Product-tree hygiene (PLAN #20): files you create outside docs/factory/ must not contain the substrings dcg or ryk in any case. Pin at vendor/parity/PIN. Extractor placeholder at tools/extract-packs/. PARITY.md and EXTRACT.md use the spec’s field names (Upstream, Version, Git tag, Tag object, Commit). AGENTS.md says “do not write foreign product names into this tree” — do not put those two tokens into AGENTS.md.

Stop in one sentence if uname -m is not arm64, or if swift --version is not 6.2.x.

Do this in order:

1. Write Package.swift exactly as the T0 spec graph: package name rv, platforms macOS 26 only, twelve library products, twelve test targets, RVPacks resources [.copy("Resources/packs")], swiftLanguageModes [.v6], empty dependencies. If .macOS(.v26) does not parse, the only fallback is .macOS("26.0").
2. Write one empty public enum per module under Sources/<Module>/<Module>.swift, each with the arm64 #error guard from the spec. RVHistory.swift also gets one short comment: stub; history stays off by default; do not persist command text. No Date(), FileManager, ProcessInfo, TTY, XPC, regex, or pack parsing.
3. Write one Swift Testing smoke test per *Tests target, same shape as the spec’s emptyModule_compiles example. No XCTest.
4. Create the reserved empties: Sources/RVPacks/Resources/packs/.gitkeep, Tests/RVEngineTests/Fixtures/corpus/.gitkeep, vendor/parity/EXTRACT.md (upstream→dest map only; “no extractor in T0”), tools/extract-packs/README.md (entrypoint only; no fetch/parse script).
5. Write vendor/parity/PIN with these keys and values:
   source=https://github.com/Dicklesworthstone/destructive_command_guard
   version=0.11.0
   tag=v0.11.0
   tag_object=6d4fcaef45d6b207a291158dc4077e54e6be685c
   commit=2ed7eeef1ae63d204495f02312c657dd6d9bf73d
6. Write .gitignore with .build/, .swiftpm/, xcuserdata/, .DS_Store. Do not ignore Package.swift or docs.
7. Write README.md from the T0 spec row: job, names (rv / rvd / RV_ / ~/.config/rv/), platform, hosts (Pi / Grok / OpenCode), pinned 0.11.0 decisions + rule_id, license deferred. No live-HOME install steps. No foreign product names.
8. Copy the six PLAN style-contract bullets verbatim into AGENTS.md and docs/dev/SWIFT.md. AGENTS.md also gets the spec’s extra bullets (this repo only, macOS 26 arm64, hexagonal arrows down, T0 serial then T1, pointers, condensed forbidden list). docs/dev/SWIFT.md also gets Sources/<Module>/ and Tests/<Module>Tests/ layout and Swift Testing.
9. Write docs/architecture/MODULES.md as one page: PLAN’s Owns / Must not table for all twelve modules, this spec’s dependency graph, RVHistory marked stub.
10. Write docs/dev/PARITY.md with the spec’s pin table and the evaluation order sentence (for later tickets; do not implement evaluate).

Prove each item with a command or a file read. A missing file is a fail, even if swift test is green:

- [ ] uname -m is arm64; swift --version is 6.2.x
- [ ] swift build succeeds
- [ ] swift test is green; all twelve *Tests targets ran
- [ ] swift package dump-package lists exactly the twelve library products and twelve test targets, no executables, no package dependencies
- [ ] Package.swift platforms are only macOS 26
- [ ] each Sources/<Module>/<Module>.swift has the arm64 #error guard and no product logic
- [ ] RVHistory.swift has the stub comment
- [ ] AGENTS.md and docs/dev/SWIFT.md contain the six style-contract bullets verbatim
- [ ] README.md, .gitignore, MODULES.md, and PARITY.md exist and match the spec rows
- [ ] vendor/parity/PIN has version=0.11.0, tag=v0.11.0, tag_object and commit as above
- [ ] packs/ and corpus/ contain only .gitkeep; tools/extract-packs contains only README.md; EXTRACT.md has no Rust and no pack JSON
- [ ] test ! -e LICENSE
- [ ] rg -i --glob '!docs/factory/**' 'dcg|ryk' is empty
- [ ] docs/factory/PLAN.md is unchanged

If a prove item fails, fix it and re-run that prove. Do not start T1. Do not git commit unless the human asked.

When every box is proven, write one short human paragraph: what you created, the exact swift test result, that T0 is only the empty scaffold (hosts are not wired; git reset --hard is not evaluated yet), and that T1 is next. Do not claim the day-one win.

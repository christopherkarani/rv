**Maint ticket.** Current board: `docs/factory/STATUS.md`. Spec: `docs/factory/specs/phase-5-size-speed.md` (T13 section).

You are a senior Swift 6.3 engineer implementing rv ticket **T13** (allow-path I/O) in a dedicated git worktree of /Users/chriskarani/CodingProjects/rv. Keep working until every T13 checkbox in that spec is proven.

Read in order: `AGENTS.md`, `docs/factory/STATUS.md`, `.grok/skills/swift-hook-xpc`, `docs/factory/specs/phase-5-size-speed.md`. PLAN.md wins product-law conflicts as arbiter.

**Blocked by:** none. Parallel with T10, T11, T12. Branch: `feat/t13-allow-io`. Do not share a working tree.

**Owns:** `Sources/RVService/GatedEvaluate.swift`, `Sources/RVService/PacksFacade.swift` (`effectiveIDs` only), `Tests/RVServiceTests/GatedEvaluateTests.swift`, PacksFacade tests (existing or new under `Tests/RVServiceTests/`).

**Must not:** edit `EvaluateSession` compile policy, `ServiceRuntime` hello/dispatch, `Sources/rv`, `RVIPC`, `install.sh`. Do not skip honor on deny. Do not treat indeterminate as allow. Do not redesign analytics.

Work only in rv. Do not write the tokens dcg or ryk (any case) into any file outside docs/factory/.

On `.allow` and `.indeterminate`, `GatedEvaluate.apply` / `peek` return the engine result without `AllowlistStore.loadUserSnapshot` or `store.consume`. On `.deny`, today’s Policy gate (allowlist, then grant if cwd present) stays. `PacksFacade.effectiveIDs`: empty home or missing `config.toml` → `dayOnePackIDs` without loading the pack index; existing config still expands extras.

Prove with `tools/gate.sh --quiet RVServiceTests RVCLITests`. End with a short human paragraph: what I/O an allow hook no longer does, and that a grant still unlocks one deny. Do not commit unless the human asked.

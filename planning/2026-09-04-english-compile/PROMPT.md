Paste this entire file as the first message of a new session in `/Users/chriskarani/CodingProjects/rv`. Do not summarize it first. Implement **W1 only**. Do not re-plan.

---

You are implementing the locked **English compile W1** program in this repo only.

**Zig `implementor` is forbidden.** This is Swift. Prefer workflow `english-compile-swift` with `args.wave=W1`. If you are a single agent, follow the program plan and exclusive writes anyway.

**This human message names this program.** Do not start OPE-264 Host Ask, companion app, live Auto-review, MCP `ProposedAction`, or anything else on `docs/architecture/02.md` § Order except the one-line pointer in w1-law. Branch `feat/english-compile` from current `main` (or a worktree of `main`).

Read in this order, then code. Do not re-derive acceptance:

1. `AGENTS.md`
2. `planning/2026-09-04-english-compile-implementable-program.md` — **wins** waves, exclusive-writes, reject list
3. `planning/2026-09-04-english-compile/waves/W01-typed-form.md`
4. `planning/2026-09-04-english-compile/HANDOFF.md`

Skills before coding: `.grok/skills/swift-feature-implementation`, `.grok/skills/swift-hexagonal-spm`, `swift-testing-pro`, `swift-functional-core`, `swift-evaluate-parity` when touching evaluate/Decision. Do **not** load Zig skills. Do **not** load `thermo-nuclear-code-quality-review`.

Gate: `tools/gate.sh` via `tools/swift-6.3.3`. Keep `.build` warm. Do not `swift package clean`. Swift Testing only. No live HOME. Value types. No `class` in Domain/Engine/Policy. No `try!` / `!` on production paths. Do not commit unless I ask.

W1 ships: product-law doc → `PolicyPredicate.gitPush` → `TypedRule` → `PolicyMatch` → engine restrict-only → store → explanation ruleID → `rv policy show`.

English compiler, AFM, npm, MCP, hook Auto-review: **not W1**.
